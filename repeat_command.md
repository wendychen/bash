# Repeat Commands

## !!

重新執行最後一條指令1次

## !n

`!n` 代表重複第n條指令.

`!2023` 代表重複第2023條指令

重新執行第2023行指令1次

## !string

!string 是重複某一個開頭的指令

例如說, 輸入指令 `history` 之後, `!for` 就可以找最後一個for開頭的指令.
`!mk` 找最後一個mk開頭的指令. `ec`找最後一個ec開頭的指令.

## 方向鍵

在輸入指令的介面, 按 up arrow key 或者 down arrow key 可以翻找指令

## Ctrl + R: 搜索歷史命令

Ctrl + R -> 輸入關鍵字

按 Enter 執行, 按 Right arrow key 編輯
